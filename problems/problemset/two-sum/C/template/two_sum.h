#ifndef TWO_SUM_H
#define TWO_SUM_H

/* Find two indices i, j such that nums[i] + nums[j] == target.
   Write the indices into *out_i and *out_j. */
void two_sum(const int *nums, int n, int target, int *out_i, int *out_j);

#endif
