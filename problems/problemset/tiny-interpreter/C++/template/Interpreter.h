#include <bits/stdc++.h>

// Execute a program in the tiny language and return all printed output
// as a newline-separated string.
//
// A clean solution will likely split into Lexer, Parser (AST), and
// Interpreter files — add as many .h/.cpp files as you need.

class Interpreter {
public:
    std::string run(const std::string& source);
};
